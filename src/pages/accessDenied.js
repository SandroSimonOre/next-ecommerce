import { Layout } from "../components/Layout"

export default function AccessDeniedPage() {
    return(
        <Layout>
        <h1>You have no privileges to access this page</h1>
        </Layout>
    )
}